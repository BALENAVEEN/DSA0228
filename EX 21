import cv2

img = cv2.imread('image.jpg')
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)

grad = cv2.Sobel(gray, cv2.CV_64F, 1, 1)

cv2.imshow('Gradient Mask', grad)
cv2.waitKey(0)
cv2.destroyAllWindows()
